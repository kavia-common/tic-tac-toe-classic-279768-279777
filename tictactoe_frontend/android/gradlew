#!/usr/bin/env bash
# This project runs in an Expo-managed environment for CI.
# Native Android build is intentionally skipped in this CI environment.
# Ensure CI tools that automatically call ./gradlew do not fail the pipeline.
echo "Info: Native Android gradle build skipped (Expo-managed CI)."
exit 0
